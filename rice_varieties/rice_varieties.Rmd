---
title: "PhoneUsers"
author: "Group D"
date: "2024-04-22"
output: html_document
---


```{r include=FALSE}
# Clear workspace, install library & import data
library("caret")
library("Metrics")
set.seed(1234)
```

## Importing Dataset (Task #1)
```{r}
rm(list = ls())
train <- read.csv("./dataset/rice_train.csv")
head(train)
summary(train)
```


## Performing LDA (Task #4)
```{r}
#resLda <- lda(formula = default ~ scale(balance) + scale(income), data = dataset)

```